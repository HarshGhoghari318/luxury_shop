import { activeContext } from "@/utils/Context";
import React, { useContext } from "react";

function Shop() {
   const {setActive} = useContext(activeContext)

  return (
    <>
    
    
    </>
  );
}

export default Shop;
